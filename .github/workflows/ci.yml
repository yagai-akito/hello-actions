name: Hello CI

on:
  push:
    branches: [ master ]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Say Hello
        run: echo "Hello, World!"